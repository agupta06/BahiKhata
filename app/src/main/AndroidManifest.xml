<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.resolvebug.app.bahikhata">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.resolvebug.app.bahikhata.GlobalOperations"
        android:allowBackup="true"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.resolvebug.app.bahikhata.HomeActivity" />
        <activity
            android:name="com.resolvebug.app.bahikhata.LoginActivity"
            android:configChanges="orientation"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity to start -->
        <!-- <activity -->
        <!-- android:name=".FirebaseCrud" -->
        <!-- android:configChanges="orientation" -->


        <!-- used when you want to make the activity as the exit out of app activity -->
        <!-- android:launchMode="singleTop" -->


        <!-- android:noHistory="true" -->
        <!-- android:screenOrientation="portrait" -->
        <!-- android:theme="@style/Theme.Transparent" -->
        <!-- android:windowSoftInputMode="adjustPan"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->


        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->

        <activity
            android:name="com.resolvebug.app.bahikhata.Themes"
            android:configChanges="orientation"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="com.resolvebug.app.bahikhata.FirebaseCrud"
            android:configChanges="orientation"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"
            tools:replace="android:value" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <!-- Broadcast receiver declaration in manifest file and make sure to enable it -->

        <activity android:name="com.resolvebug.app.bahikhata.MainActivity" />
        <activity android:name="com.resolvebug.app.bahikhata.WelcomeScreen" />
        <activity android:name="com.resolvebug.app.bahikhata.ThemeTwo"></activity>
    </application>

</manifest>